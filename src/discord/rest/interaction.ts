import type DiscordRest from "../core/rest";

export default class RestInteractionRouter {
  #api;

  public async submitSingleGuildCommand( ) {
    
  };

  public async submitMultiplesGuildCommands( ) {
    
  };

  constructor(rest: DiscordRest) {
    this.#api = rest;
  };
};